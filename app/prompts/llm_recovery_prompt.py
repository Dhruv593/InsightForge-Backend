STRUCTURED_RETRY_PROMPT = """The previous response failed validation. Return a complete JSON object matching the supplied response schema. Include all required fields, use the exact allowed field names and enum values, and preserve verified column and evidence identifiers. Use JSON null only where allowed, never the string 'None' as a column name. Do not invent missing facts to satisfy the schema. Return no Markdown, code fences, or extra prose."""
